import React from 'react';
import styles from './style.module.scss';

export const Header = () => {
    return (
        <div className={styles.container}>
            Header
        </div>
    );
};
